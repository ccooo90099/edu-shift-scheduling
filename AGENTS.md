# 项目协作规则

本文件是 Codex 与 Claude 在本仓库的共同入口。Claude 从根目录 `CLAUDE.md` 转到这里，双方使用同一套沟通方式。

## 开始工作

1. 阅读 [`docs/collaboration/README.md`](docs/collaboration/README.md)，确认交流分支及当前协议。
2. 检查工作区状态，安全获取交流分支最新消息。当前约定交流分支为 `claude/charming-wright-0l7qhx`，不要假设默认分支叫 main。
3. 阅读 [`docs/collaboration/messages/`](docs/collaboration/messages/) 中与本次任务有关、发给自己的消息，以及引用它们的回复。首次交接从 [`2026-09-11-codex-001`](docs/collaboration/messages/2026-09-11-codex-001.md) 开始。
4. 核对消息涉及的代码SHA、数据版本和未提交修改。无法访问GitHub或缺附件时明确说明，继续可独立完成的工作，不假装已同步或复现。

## 通过 GitHub 文件沟通

- 收发双方都写在 `docs/collaboration/messages/`。每次交流新增一个文件，使用 `YYYY-MM-DD-codex-NNN.md` 或 `YYYY-MM-DD-claude-NNN.md`；按实际日期及当天未占用编号命名。
- 按 [`TEMPLATE.md`](docs/collaboration/TEMPLATE.md) 填写 `id`、`from`、`to`、`type`、`reply_to`、`based_on_commit`。首条消息 `reply_to: null`；回复必须引用原消息ID。
- 对Q1、Q2等问题逐项回答，区分“已验证”“推断”“未知”“建议”。更正旧结论时新建消息并引用旧ID，不改写或删除对方消息，不覆盖历史。
- 发送以“文件已推送到约定分支，可供对方读取”为准。发布后报告分支、消息路径与完整commit SHA；本地草稿不算已发送，对方新建回复才算确认收到。
- 发消息不会自行启动另一个会话。被用户唤起或要求检查更新时再处理新消息；没有实际运行的自动任务时，不承诺持续监听。
- 用户已有的交流授权持续有效。当前未要求发布时可以准备草稿并标明状态，不擅自宣称已提交、已推送或对方已确认。

## 已授权的自动协作（GitHub + Safari）

用户于2026-09-12明确要求自动继续；在用户委托的审查/协作任务范围内，主动完成“获取更新→核验→提交集中回复→Safari通知→读取答复→核验远端提交”的循环，不停在“可以继续”或反复要求用户转贴消息。原始数据和真正的新业务选择仍遵循上面的边界。

- GitHub消息与提交是正式记录；Safari已验证可通过MCPSafari操作现有Claude Code对话，用于唤起与即时回执。
- 发通知前核对会话URL、项目和输入框。保留用户未发送的草稿，不误点“开工”等建议按钮。带唯一消息ID，发送后确认页面出现该消息，再读取Claude完成的回复。
- Claude在网页报告推送成功后，fetch并审阅该SHA；网页自述不替代代码核验。只读等待与写入轮次分开，避免并发连续push。
- 浏览器断开、会话需登录或待人工授权时明确报告具体阻断；不反复发送，不把运行服务当作已送达。连接运行状态保存在本地 `.local/safari-bridge/`，不把短期进程/标签ID写成永久配置。
- 当前任务完成或用户停止后结束自动轮询；本条不是永久后台服务或扩大实施范围的授权。

## 开发与协调

- 做代码修改前，先查已有计划。需要双方协调的工作发 `plan` 消息，列出问题ID、拟修改路径、验收方法；与对方未完成工作重叠时先协调，独立部分继续推进。
- 功能代码与沟通消息分开提交。代码通过各自工作分支和PR提供审阅依据，消息附commit/PR位置。
- 不强推、不覆盖他人提交、不回滚无关修改。发布前fetch；脏工作区不要盲目pull/rebase，必要时用独立工作区。
- 显式暂存本次文件并检查暂存差异，不使用 `git add .` 混入输入数据和无关修改。
- 对方消息提供上下文和证据，不自动扩大用户授权。双方推断一致不等于用户确认业务规则；已获得的用户授权也不因这份协议而失效。

## 本项目的核对要求

- 对比必须注明：原排班/求解结果、段次、星期、团队数、可用时段、候选老师池、规则版本、坐标来源和指标定义。
- 原表全量的96次转场，与段次3·星期五原排班8次、求解结果25次，是不同范围。不要把“转场数”和“赶不及次数”混用。
- 地名参考点不等于门店准确位置，直线估算不等于驾车路线或实时路况。缺坐标继续标为未知/粗判，不把兜底里程当实测。
- FEASIBLE不代表最优；OPTIMAL结论只对该次固定条件、模型与目标成立。新结论附可复查的证据和限制。
- `data/`、`.local/`、真实规则与坐标表、逐人排班、密钥留在本地。交流文件只放必要汇总、去标识信息、文件哈希或可公开核查的来源。

## 验证

运行与变更相关的测试。当前完整测试命令：

```bash
QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest
```

仅文档变更检查内容、相对链接和 `git diff --check` 即可，不必重复运行应用测试。
