"""领域层 —— 纯业务，零框架依赖。

这一层不 import pandas、ortools、fastapi、sqlite 中的任何一个。
判断标准很简单：换掉求解器、换掉数据库、换掉界面，这一层一行都不用改。

分三块：
  model/   领域模型 —— 实体与值对象，业务语言的载体
  policy/  领域策略 —— 可配置的业务规则（连堂、转场、降级、黄金时段）
  service/ 领域服务 —— 跨模型的业务逻辑
"""
