# 排班系统 · 业务需求（v1.0）

> 想快速了解业务的，先看 [`业务理解.md`](业务理解.md)（精简版，只写结论）。本文是完整版，含数据证据、推导过程和历次修正记录。
> 本文与 [`架构与功能逻辑.md`](架构与功能逻辑.md) 是当前**权威**文档。
> `需求理解.md` 是最早的探索稿，`转场次数口径与结果对照.txt` 是单次实验记录，两者只作历史保留，与本文冲突时以本文为准。
>
> 每条规则都标注了来源：
> **【用户定案】** 用户明确说过的业务规则 ·
> **【已验证】** 从原始表复算得出、可重跑核对 ·
> **【推断】** 由事实推得但未经用户确认 ·
> **【未知】** 缺规则或缺数据，**不得由实现方替用户定案**

---

## 0. 业务背景

深圳某**教培**机构（**不是托管/晚托**）的分校排课。学生按报名科目来上课，课间不留校，午休由家长接走。

现状是人在 Excel 里把两百多个团队手工拖进「中心 × 时段」的格子里。本系统要把这件事反过来做：**给定要开的课 + 可用的指导员 + 规则，自动排出这张表**，并给出看板视图和冲突报告。

```
输入：待排团队清单 + 指导员池 + 场地 + 规则参数 + 场景（寒暑假/春秋季）
  ↓  CP-SAT 求解
输出：① 排班明细（列结构与现有导出一致，可回灌体检）
      ② 中心 × 时段 看板
      ③ 指导员个人课表 + 工作量统计
      ④ 冲突 / 未排上 报告（含牺牲了哪些软约束）
```

---

## 1. 领域概念

### 1.1 产品 ↔ 科目 ↔ 年级 【用户定案】

| 产品名 | 实际科目 | 开设年级 |
|---|---|---|
| 编程理论 | 数学 | S7 起 |
| 双语文化 | 英语 | S7 起 |
| 文学美育 | 语文 | S7 起 |
| 躬行实践 | 物理 | **S8 起** |
| 溯源 | 化学 | **S9 起** |

即：初一语数英三科；初二加物理；初三加化学。产品名是内部叫法，排课逻辑上就是科目。

> **年级 → 可开产品是一条硬约束**：S7 不应出现躬行实践/溯源。
> 【已验证】原表中 S7 确实没有这两个产品的行。

### 1.2 程度（年级） 【用户定案 + 已验证】

`程度` = 年级。当前项目取值 **S7 / S8 / S9**（初一/初二/初三）。

一个学生只属于一个程度，且跨产品不变 —— 所以**程度是"同一撮学生"的判定字段**，这是 H2 冲突口径的基础。

配置项：`冲突口径.分组字段`，默认 `[校区, 程度]`。

### 1.3 团队类型（层级） 【用户定案】

| 代码 | 全称 | 用户确认 |
|---|---|---|
| LI | 励学 | "利" = LI 励学 ✅ |
| LE | 乐学 | "绿" = LE 乐学 ✅ |
| BO | 博学 | KT/KD 指 KP-BO ✅ |
| CX | 创新 | KT/KD 指 KP-CX ✅ |
| ZY | 卓越 | — |

这是**能力分层**。用户原话："程度必须匹配，利和绿，乐和乐，KT 跟 KD"——即**排在一起的课，层级要一致**，不能把励学班和乐学班的学生混着连堂。

### 1.4 中心 vs 校区 【已验证 · 关键】

30 个**中心**实际落在 20 个**校区**上：`百花科学` 和 `百花文学` 是同一个校区的两栋楼。

- **校区**是学生感知的地点 —— 冲突判定、"同地点"判定都按校区
- **中心**是排课和场地的单位 —— 教室归中心
- 派生规则：中心名去掉 `科学` / `文学` 后缀 = 校区名

【已验证】文学中心只上文学美育（26/26 行）；科学中心和综合中心什么都上。所以「产品 → 可用中心」本身就是一条硬约束。

### 1.5 期位 —— 两个场景共用的同一个维度 【用户定案 · 关键简化】

暑假**常规班分两期**：第一期、第二期。目标是**每期都尽可能覆盖所有年级 × 所有科目**，覆盖不了的才放到第二期。

**而春秋季的周六 / 周日，就是这两期平移过去的结果**。用户原话：

> "假如说是暑假有两期的话，那么把第一期的课程通常就会平移到周六去上课……第二期的课程通常会平移到周日上课。"

所以两个场景不需要两套模型，只有**一个抽象维度**，在不同场景下换个名字：

| 抽象维度 | 寒暑假 | 春秋季 |
|---|---|---|
| **期位 A** | 第一期 | **周六** |
| **期位 B** | 第二期 | **周日** |

这带来三个直接结论：

1. **「拆到两天」和「推到第二期」是同一个降级动作** —— 都是"把排不下的科目挪到另一个期位"。§4.5 的两张降级表其实是一张。
2. **周六 / 周日 不是随便挑的**（修正了我先前的推断 U12）—— 一撮学生在假期里是第一期，春秋季就接着上周六。**期位是跟着学生走的，有延续性。**
3. 模型里不需要「期」和「星期」两个维度。给团队一个**期位变量**就够了，渲染成"第一期"还是"周六"是输出层的事。

> **【未知 U9】** 原表的 `段次1–4` 与期位的对应关系。是"常规两期 + 促销两期"，还是别的切法？
> **【未知 U14】** 一个**新生**（没有上过假期班、没有历史期位）怎么定期位？是自由分配，还是有别的规则？
> **【未知 U15】** 学生能不能**中途换期位**（本来周六，下学期改周日）？如果不能，这是硬约束；如果能，是软约束。

### 1.6 团队 = 一个班

一行明细 = 一个团队 = `(中心, 程度, 产品, 团队类型, 班序)`，有固定人数（【已验证】均值 15.9，容量 24），要被分配到一个 `(日期/星期, 时段, 主指导员, 指导室)`。

---

## 2. 两个场景 【用户定案】

**两个场景的排课逻辑是同一套** —— 用户原话："寒暑假跟周末，平时周末其实一样的"。
区别只在两处：**时段表**，以及**「一个批次由哪些天组成」**。真正的业务难点在两边是同一个：排不下的时候往哪儿退（见 §4.5）。

### 2.1 寒暑假场景

- 一个批次**连上 7–8 天**，中间不休；若拉到 15 天，中间会休息一次
- **不区分星期几** —— 不存在"只有周六上"这回事，每天都是上课日
- 全天开课，用完整时段表

【已验证】原表四个段次全部符合该结构：

| 段次 | 推算首次上课日 | 该日星期 | `周` 字段 | 末次 | 次数 |
|---|---|---|---|---|---|
| 段次3 | 2024-08-02 | 星期五 | 星期五 | 2024-08-11 | 9 |
| 段次4 | 2024-08-16 | 星期五 | 星期五 | 2024-08-25 | 9 |
| 段次2 | 2024-07-19 | 星期五 | 星期五 | 2024-07-28 | 9 |
| 段次1 | 2024-07-06 | 星期六 | 星期六 | 2024-07-16 | 9 |

> **`周` 字段是「首次上课那天是星期几」，不是「每周星期几上课」。**
> 这一点此前被双方误读，导致把 `(段次, 周)` 当成互不相干的子问题（见 §7 R1）。

### 2.2 春秋季场景

- **周末两天为主**（周六、周日都开课）
- 偶尔有**周五晚上**的课，且**周五只开 18:30-20:30 这一档**
- 指导员作息：**周一、周二休息；周三/周四/周五教研**

### 2.3 时段表 【用户定案】

| 时段 | 寒暑假 | 春秋季 周六/周日 | 春秋季 周五 |
|---|---|---|---|
| 08:10-10:10 | ✅ | ✅ | ✗ |
| 10:30-12:30 | ✅ | ✅ | ✗ |
| 14:00-16:00 | ✅ | ✅ | ✗ |
| 16:20-18:20 | ✅ | ✅ | ✗ |
| 18:30-20:30 | ✅ | ✅ | ✅ **仅此一档** |

> 原表里还出现过 `13:30-15:30`，与 `14:00-16:00` 重叠。**【用户定案】暂不考虑这一档**，从时段表移除。
> 但**时段重叠的判定逻辑要保留** —— 时段表是可配置的，将来加回重叠档时不能再踩一次 `p1 < p2` 单向判定那个坑。

### 2.4 时段间隙结构 【已验证 + 用户定案】

```
08:10-10:10 → 10:30-12:30    空 20 分钟   ← 课间
10:30-12:30 → 14:00-16:00    空 90 分钟   ← 午休
14:00-16:00 → 16:20-18:20    空 20 分钟   ← 课间
16:20-18:20 → 18:30-20:30    空 10 分钟   ← 课间
```

用户确认："**午休本身是有 120min 间隔，但上午和下午之间课程之间的间隔就是按照目前数据里面推算出最小间隔即可**"。

系统自动从时段表推导"课间 vs 休息"的分界：找相邻等待时间序列 `[10, 20, 50, 60, 90]` 里最大的比值跳变（20→50 是 2.5 倍），得出**上限 = 20 分钟**。10/20 分钟算课间，50 分钟以上算休息。

---

## 3. 硬约束（违反 = 排班不可用）

| # | 约束 | 来源 | 原表执行情况 |
|---|---|---|---|
| **H1** | 一个指导员同一时段只能带一个团队 | 常识 | 【已验证】批次内 0 违反。跨批次另有 **21 处估算重叠候选**（按推算日期得出，**真实授课日未证实**），见 §7 R1 |
| ~~**H2**~~ | ~~同时段 + 同校区 + 同程度 + 不同产品 不能同时开课~~ | **已降级为软约束 S8**，见 §3.1 和 §4.6 | 原表 409 组里 164 组"违反"—— **那不是违反，是有意为之** |
| **H3** | 同时段同中心并发团队数 ≤ 教室数 | 场地上限 | 【已验证】有教室数据的行 0 违反 |
| **H4** | 只能把指导员排到他能教的产品上 | 资质 | 【已验证】106 人中 103 人只教 1 个产品；唯一跨产品组合是「溯源+躬行实践」（3 人） |
| **H5** | 年级 → 可开产品（S7 无物理/化学） | 【用户定案】§1.1 | 【已验证】原表符合 |
| **H6** | 指导员/中心/时段可用性黑名单 | 请假、限定中心 | **【未知】**：需要新增输入，当前无数据 |

### 3.1 H2 的降级 —— 我此前把它定错了 【重大修正】

**先前的写法（错的）**：H2 是硬约束，是本系统最大的价值点，原表 40% 违反是需要修的问题。

**用户澄清（对的）**：

> "我们这个教室是没办法……很难兼顾到把时间所有的都去平铺开来、不冲突的，做不到。那可能就是必须要冲突啊，必须要同一个时间段上两个课或者是三个课。那些学生他可能并不是在读三科，可能就读了两科啊，就只有数学物理，或者是只有数学语文这种，那么他 10 点钟的这两科也不会有什么冲突。"

用户举的例子：

```
08:10-10:10  数学
10:30-12:30  语文  ┐ 同一时段两个产品
10:30-12:30  物理  ┘ —— 这是允许的
```

**为什么原表那 40% 不是错误**：

1. **教室和时段容量根本不够**把所有产品平铺开。硬要不冲突，就是无解。
2. **不是所有学生都读全科** —— 只读「数学+物理」的学生，10:30 那格同时开语文和物理并不影响他。
3. 还有一部分学生家长时间凑不齐，**主动只选一科**（用户原话："他可能会去选其中的一科，会去相应的一些取舍"）。

**真正的约束是什么**：不是"同程度的不同产品不能同时段"，而是——

> **同一个学生实际报读的那几科之间不能撞时段。**

这是一个**窄得多**的约束。它的代价正比于**同时报了这两科的学生人数**，而不是"存在两个不同产品并发"这个事实本身。

**这个错误的实际后果**：我先前的求解器把一个软约束当硬约束建模，问题被**过度约束**了 —— 这很可能就是它排不开、转场次数离谱（31 次/天）、140 秒不收敛的根因之一。已登记为缺陷 **R7**。

> **实现注意仍然有效**【已验证】：时段之间存在时间重叠（13:30-15:30 × 14:00-16:00），并发判定必须对**所有不相等的产品对**双向做，只做 `p1 < p2` 单向会漏掉 8 处。降级成软约束后这条同样适用。

---

## 4. 软约束（可加权取舍）

### S1 连堂 —— 学生视角 【用户定案】

用户原话（学生视角）：一个学生报语数英三科，希望三节课能挨在一起上完，而不是"早上 8 点上一个，下午两点到 4 点上一个，4 点到 6 点再上一个"。

**规则细节**：

1. **配对优先级由实际报读组合驱动，不是固定顺序** 【用户定案 · 修正】
   常见搭配是 `数学+语文`、`数学+英语`、`语文+英语`，**但也有很多是 `数学+物理`**。用户原话：

   > "我们排的时候肯定是先排一种可能嘛，对吧？然后呢，如果说他后续比如说上了数学物理的学生比较多，那可能会把数学物理放在一起去排，就是放在同一个上午或者同一个下午这种时间连续的。"

   → 这是一个**两阶段**的过程：先按默认顺序排一版，再根据**实际报读组合的人数分布**调整，把人多的组合优先安排连堂。
   → 所以配对权重应当是**输入数据**（各科目组合的学生人数），而不是写死在配置里的顺序。缺这份数据时才退回默认顺序 `数学+语文 > 数学+英语 > 语文+英语`。
   → **【未知 U16】** 各科目组合的学生人数，这份数据拿得到吗？拿不到的话，能不能用"上一期的排班结果 + 各班人数"近似？
2. **层级必须一致** —— 连堂的两节课必须同校区、同程度、同团队类型（§1.3）
3. **跨午休算连堂** 【用户定案 · 修正了旧文档】
   用户原话："跨午休到底也算连，尽可能一个半天，比如一个上午或者下午，实在不行只能分散，这也是次优选项了"
   → 三档偏好，从好到差：
   - **① 同半天连排**（8:10→10:30，或 14:00→16:20→18:30）—— 最优
   - **② 跨午休连排**（10:30→14:00）—— 可接受，次优
   - **③ 分散**（中间隔了别的时段）—— 次优选项，能排上就行
4. **只看当天** 【用户定案】用户原话："当天连在一起即可"
   → 连堂判定**不跨日**。日期/星期由场景和批次决定，不是决策变量。

> ⚠️ **当前实现与本节不符**，见 §7 R2：现有代码把 `最大间隙_分钟: 自动` 推出的 20 分钟当硬门槛，等于把"跨午休"判为不连堂——与用户定案相反。

【已验证】原表现状：真正同半天挨着的只有 **38%**（51/135）；另有 22% 是跨午休的 10:30→14:00。按新口径，实际达标率是 60%。

### S2/S3 转场 —— 指导员视角 【用户定案】

用户原话（老师视角）的反面教材："8 点在宝安带，10 点又到南山去，下午两点又跑到宝安去，4 点又跑到南山，这样他就太折腾。"

| # | 约束 | 说明 |
|---|---|---|
| S2 | 指导员当天尽量固定在一个中心 | 【已验证】原表 72% 的老师当天只在 1 个中心 |
| S3 | 必须跨中心时，越近越好，且要留够路上时间 | 优先级：同校区 > 同区域 > 跨区域 |

**转场计数口径** 【用户定案 + 已与 Codex 对齐】：按**相邻课之间实际发生的中心变更**计数。`A→B→A` 记 **2 次**，不是"去过 2 个中心 = 1 次额外"。里程按每次事件累加。

> ⚠️ **当前实现口径不一致**，见 §7 R4：求解目标算的是"额外活动中心数"（distinct − 1），体检算的是"实际转场事件"。同一份排班一个报 19 一个报 25。

**赶路时间判定**：`可用间隔 ≥ 通行时间 + 安全余量`。数据来源按可信度排队：

| 顺序 | 来源 | 怎么来的 |
|---|---|---|
| ① | `config/travel.csv` | 真实驾车时间/里程，地图 API 跑一次 |
| ② | 经纬度直线估算 | 直线 km × 折算系数 ÷ 车速 + 固定开销 |
| ③ | 同校区 / 同区域 / 跨区域 档位 | 连坐标都没有时的兜底，报告里明确标出 |

> **兜底必须保守**：区域未知的一律按"跨区域"算。让两个 `None` 相等而判成"同区域"，会因为缺数据反而**放宽**约束。

【已验证】原表有 24 次转场只有 10–20 分钟间隔（例：10 分钟内从罗湖翠竹赶到福田东海城市广场），在地图上不可能完成。

### S4–S6 其他

| # | 约束 | 状态 |
|---|---|---|
| S4 | 指导员当天课表少空档 | 【已验证】227 个多节课人次里 170 个有空档 |
| S5 | 工作量均衡（惩罚峰值） | 【已验证】人均 6.2 个团队，最多 13 个 |
| ~~S6~~ | **已移出软约束** —— 促销班的黄金时段限制是**条件性硬排除**，不是可被抵消的罚分。见 §4.9 |

---

## 4.5 排不下时的降级链 【用户定案】

§4 S1 讲的是"能排下的时候怎么排得好"。本节讲**排不下的时候往哪儿退** —— 这是用户强调的实际痛点，两个场景通用。

### 4.5.1 为什么会排不下

一撮学生（= 同校区 + 同程度 + 同层级，即 §3 H2 的分组口径）要上的科目数是固定的：

| 程度 | 科目数 | 科目 |
|---|---|---|
| S7 | 3 | 语文、数学、英语 |
| S8 | 4 | + 物理 |
| S9 | 5 | + 化学 |

而一个校区一天能给这撮学生开的节数是有限的 —— 受教室数、可用师资、时段数共同约束。用户原话："**我们这个校区周六可能只有两节**"。3 门课塞不进 2 个位置，就必须降级。

> **【未知 U11】** "一个校区一天最多给一撮学生开几节"这个上限，是从教室数和师资**算出来**的，还是有人另外**规定**的一个数？如果是后者，需要作为输入项。

### 4.5.2 降级顺序（从好到差）

因为期位是两个场景共用的同一个维度（§1.5），降级链**只有一张表**：

| 档 | 安排 | 寒暑假读作 | 春秋季读作 | 依据 |
|---|---|---|---|---|
| ① | 全部科目在**本期位内**，且同半天连堂 | 都在第一期，同半天 | 都在周六，同半天 | 最优 |
| ② | 都在本期位内，但跨午休或分散 | 同期不同天/不同半天 | 同一天但有空档 | 见 §4 S1 的第②③档 |
| ③ | **挪一部分到另一个期位** | 推到第二期 | 周六 2 节 + 周日 1 节 | 用户原话见下 |
| ④ | 彻底排不上 | — | — | 最差 |

用户对第③档的两处原话，说的是同一件事：

> （春秋季）"那我们这个校区周六可能只有两节，那只能他周日上一节了，那只能是这样子"
> （寒暑假）"那这个阶段只能安排两节，那只能让他上第二期了"

### 4.5.3 期位不是自由选的 【用户定案 · 修正了先前的推断】

先前我推断"周六周日无所谓，集中在同一天即可"（旧 U12）。**这个推断是错的**，已按用户补充更正：

> "第一期的课程通常就会平移到周六去上课……第二期的课程通常会平移到周日上课"

**期位跟着学生走，有跨季延续性**：假期上第一期的那撮学生，春秋季接着上周六。所以春秋季的期位在有历史数据时是**继承来的输入**，不是决策变量。

只有没有历史期位的新生才需要分配（见 U14）。

### 4.5.4 拆开时先拆哪一门 【用户定案】

> "语数理化放在了同一天，那剩下的语文英语可能就放在另外一天。"

→ **把人数最多的那几个组合留在同一天连堂，剩下的拆出去**。跟 §4 S1 第 1 条一样，这由实际报读组合的人数分布决定，不是固定顺序。

先前我推断"英语最容易被拆出去"（旧 U13），那只在默认顺序下成立；一旦某个校区数学+物理的学生特别多，被拆出去的就可能是别的科目。

### 4.5.5 对目标函数的影响

降级链要在目标函数里体现成一条**严格递增的惩罚序列**：

```
已连堂  <  本期位内不连堂  <  挪到另一个期位  <<  彻底排不上
惩罚 0                                              惩罚最大
```

注意 **「挪到另一个期位」不等于「排不上」**。现有实现只有"排上/没排上"两种状态，把挪期位当成失败会让求解器过度让步。这是新登记的缺陷 **R6**。

同时新增一条软约束：

| # | 约束 | 说明 |
|---|---|---|
| **S7** | 每期的「年级 × 科目」覆盖率尽可能高 | 用户定案：尽可能每期都覆盖到所有年级所有科目 |

### 4.5.6 这需要一个新的建模对象 【已按 codex-001 Q21 更正】

现有模型里只有**团队**，没有**「一撮学生」**。而 §4.5 全部规则都是围绕"同一撮学生的多门课"说的。

但**「一撮学生」不等于「按年级该上的全部科目」** —— 这是我先前写错的地方。§3.1 已经说明并非人人读全科（有人只报数学+物理，有人只报一科），如果这里又按年级把全科塞回去，等于把刚降级掉的 H2 从后门放回来。

**三个概念必须分开**：

| 概念 | 是什么 | 用在哪 |
|---|---|---|
| **年级可开科目** | S7 语数英 / S8 +物理 / S9 +化学 | 只用于 H5（不能给 S7 开物理）和 S7 覆盖率 |
| **班级供给** | 这个校区这个程度这个层级实际开了哪些班 | 排班的对象 |
| **报读组合 + 估计人数** | 「数学+物理: 约 18 人」「只数学: 约 6 人」… | **S1 连堂优先级、S8 并发惩罚、拆期代价，全部依据它** |

**修正后的建模对象**：

```
人群 = (校区, 程度, 团队类型)          ← 只表示"同类人群"，不表示"同一批人"
  └─ 报读组合[]  例如：
       {科目: [数学, 语文], 估计人数: 22}
       {科目: [数学, 物理], 估计人数: 18}
       {科目: [数学],       估计人数:  6}
```

连堂、同天、拆天、挪期位，判定单位是**报读组合**，代价按该组合的估计人数加权。一个只报数学的学生，不会因为他是 S9 就被要求把五门课排连贯。

**没有报读组合数据时怎么办**：

- **不得**用"年级全科"当默认值 —— 那是虚构需求，会重新引入 H2 那个过度约束
- 退化指标：用**历史排班里同人群各产品的班数与人数**做弱代理，并在报告里明确标注"报读组合未知，连堂优先级按历史班量近似"
- 这一状态记为 **U21**，属于"数据待采集"，不是"规则未定"

这是 R6 的主要工作量。

## 4.6 新增软约束（本轮用户补充）

| # | 约束 | 依据 |
|---|---|---|
| **S8** | **同校区同程度的不同产品，尽量不要同时段并发** —— 惩罚正比于同时报了这两科的学生人数 | §3.1 —— 由原 H2 降级而来。不是不能撞，是撞了要扣分，扣多少看伤到多少人 |
| **S9** | **周六（期位 A）优先** | 【用户定案】"周六其实更好排" |
| **S11** | **额外使用教室数尽量少** —— 口径与公式见 §4.6.1。注意名字不叫"换教室次数" | 【用户定案】"一般没有（硬性对应），但从用户习惯上来讲，课室固定对于老师和学生来讲都更友好一点，不强求，次优选" |
| **S10** | **同一校区同一程度，周六和周日都要有班** | 【用户定案】新生按程度进班，"那有可能是排在周六，有可能排在周日，那就看家长的时间或学生的时间能不能错得开"；老生也能中途换班。两天都有班，家长才有得选 |

### 4.6.1 S11 教室固定：具体惩罚什么 【按 codex-001 Q24 补齐】

先分清两种"固定"：

| 情形 | 现在的模型 | 结论 |
|---|---|---|
| **同一个团队的多次课在同一间教室** | 一个团队只做**一次**决策（`(期位, 时段, 老师, 教室)`），9 天重复同一个安排 | **天然满足**，不需要额外罚分。只有真实日历上出现例外（某天教室被占）才需要表达 |
| **同一位指导员在同一中心的不同团队之间少换教室** | 完全没有约束 | **这才是 S11 真正要惩罚的事件** |

**S11 的惩罚定义**（口径已按 codex-002 第 5 条澄清）：

`罚分 = Σ(指导员, 中心, 教学日) max(0, distinct(教室) − 1)`

**这个量叫「额外使用教室数」，不是「换教室次数」。** 两者不同：`A→B→A` 在 distinct−1 口径下记 **1**，在真实切换事件口径下记 **2**。

保留 distinct−1 口径（它够用且好建模），但**名字必须准确** —— 这正是 R4 那个错误的翻版：转场当初就是因为求解算 distinct−1、体检算实际事件，同一份排班报出 19 和 25 两个数。同一个坑不能踩两次。

聚合维度是 **(指导员, 中心, 教学日)**：跨天不累计，一位老师周六周日各用一间不同教室不算违反。

> 若业务后来要求惩罚真实换教室事件（比如中途搬教室对学生有实际干扰），改口径时**必须同步改名**，并同时更新求解与体检两侧。

**前提：模型里必须真的有教室这个实体。** 现在没有：

| 现状 | 需要变成 |
|---|---|
| `center` 只存 `教室数`（一个整数） | `room(中心, 教室ID, 座位数, 可用时段)` |
| 求解器只约束并发数 ≤ 教室数（`solver.py:6` 原话："教室不单独分配，只约束并发数不超过间数"） | 团队 → 具体教室ID 成为决策变量 |
| 输出只有中心 | 输出带教室ID |

**闭环四件事**：

1. **输入**：中心的教室清单（ID、座位数、可用时段）
2. **模型**：教室分配变量；H3 从"计数 ≤ 间数"改成"同教室同时段互斥"（更强也更准）；S11 进目标函数
3. **输出**：排班明细和看板都带教室号
4. **验收**（按 codex-002 Q27 拆成三项，避免把所有不可行都归因于旧计数错误）：

   | # | 用例 | 期望 |
   |---|---|---|
   | ① | 同质教室（都能上所有课、无容量与时间差异）+ 相同硬约束 | 引入教室身份**不改变可行性**。旧模型可行的，新模型仍可行 |
   | ② | 1 间教室 + 两个**时间重叠但标签不同**的班（`13:30-15:30` × `14:00-16:00`） | 旧 H3 错误放行；新模型判 **INFEASIBLE**。这条同时是 R10 的回归测试 |
   | ③ | 2 间教室（容量 20/40）+ 同时两个 30 人的班 | 判 **INFEASIBLE**（只有一间装得下）。这是**容量数据**带来的真实收紧，不是"有了 ID"就天然更强 |
   | ④ | 同一老师同中心两个班，教室充足 | 优先分到同一间（S11 生效） |

   > **不可行时必须能定位来源**：教室清单、容量、锁定信息任一填错都会造出假性不可行。报告要指明是哪一类限制导致的。

> **顺带一个收益**：把 H3 从计数约束换成"同教室同时段互斥"之后，座位数才有地方用 —— 现在 `centers.csv` 里的座位数字段实际上是死的，因为没有教室身份就无法把班和座位对上。

---

## 4.7 新生进班与中途换班 【用户定案】

**新生**（没上过假期班、春秋季直接进）：

> "他是会直接进，比如说周六有班那就进周六班，周日有班就进周日班。一是根据他的程度来进班，比如说他稍微好一点，那可能他就进到程度好一点的班……那有可能是排在周六，有可能排在周日，那就看家长的时间或学生的时间能不能错得开了。"

→ 两步：**先按程度和层级定进哪个班型**，**再看家长时间定周六还是周日**。
→ 对排班系统的要求：**不是**由系统给新生分配期位（那是招生环节的事），**而是**保证每个校区每个程度在两个期位上都有班可进（= S10）。

**中途换班**：

> "中途能换的，比如说他周六早上的时间不合适，那可能他换到别的班……这个校区周六日两天有别的班，他也可以有合适的，他也可以选。"

→ **U15 有答案了：能换。** 所以期位延续性是**软约束**不是硬约束 —— 我先前的实现判断是对的。
→ 换班的前提同样是 S10：同校区两天都得有班。

## 4.8 学生人数是滚动估计，不是精确输入 【用户定案】

> "这个是不确定的，你想一下每个季度、每个星期报名人数和培训人数都是变化的，有可能排课还有请假的可能。只是尽可能将已经报课的学生排好，以及即将报名的学生也安排进来。"

这改变了 S1 配对优先级和 S8 并发惩罚的**性质**：

| | 错误理解 | 正确理解 |
|---|---|---|
| 数据 | 一份要精确获取的名册 | **滚动估计值**，每周都在变 |
| 目标 | 精确拟合当前人数快照 | 把**已报课的**排好；后来的学生走 §4.8.1 的接纳流程 |
| 求解 | 一次排定 | **重排是常态**，不是例外 |

三条设计含义：

1. **目标函数不能过度拟合人数。** 权重按人数比例给即可，不要为了几个人的差异去牺牲结构性的好排法 —— 下周人数一变，那个"最优解"就不成立了。
2. **repair（修补）模式的重要性上升**，但它**不是**接纳新生的唯一通道 —— 见 §4.8.1。
3. **不留余量** —— 我此前推断"班级容量不排满、给后来的学生留位置"，**这条是错的**。用户定案：

   > "肯定优先排满已经报名的，即将报名的那就等空缺（的）教师 / 课程安排上去。"

   → **按已报名人数排满**，不预留空位。后来报名的学生按 §4.8.1 的流程接纳。

> ⚠️ 我在 claude-010 里写过"repair 是接纳新生的**唯一**通道"。**这个表述过强，已撤回**（codex-001 Q23）。绝大多数新生只是报名动作，根本不触发重排。

### 4.8.1 新生接纳流程 【按 codex-001 Q23 补齐】

四步，**只有第三步才真的动排班**：

| 步 | 动作 | 触发重排？ |
|---|---|---|
| ① | **匹配已有班** —— 找同校区、同程度、同层级、时间合适、**还有余位**的班 | ❌ 只是更新报名和容量 |
| ② | 匹配不到 → **创建待排新班**（挂起，标记为待排） | ❌ |
| ③ | **局部 repair** —— 只为这些待排新班找时段、老师、教室 | ✅ |
| ④ | 排不下 → **报告限制**：缺老师 / 缺教室 / 时段满，让人来决定加资源还是劝学生换时间 | — |

**这需要三份现在没有的数据**：

| 数据 | 用途 |
|---|---|
| 各班当前报名人数 | 第①步判断有没有余位 |
| 各班额定容量 | 同上。注意：**排满已报名需求 ≠ 每个班都要塞到额定容量** |
| 哪些安排已锁定 | 第③步的可改范围。已开课的班、已通知家长的时段不能动 |

> **「与原排班不同」加一个全局软罚分，不能保证改动小。** 这是我先前设计的一个漏洞：软罚分可以被别的目标项抵消 —— 求解器完全可能为了少一次转场就把三十个班挪位置。真正管用的是**显式锁定范围**：把不可改的团队直接固定成常量，只让待排的班和少数明确允许调整的班进入决策。罚分只用于在允许调整的那部分里做偏好。

---

## 4.9 黄金时段与促销班 【用户定案 · 按 codex-001 Q25 更正】

### 黄金时段的定义

**只看一天当中的时间，不看星期几；两个场景用同一套定义。**

```yaml
黄金时段: [08:10-10:10, 10:30-12:30]   # 寒暑假与春秋季通用
促销班:
  允许占黄金时段: false
```

### 促销班的候选时段 = 当天允许时段 − 黄金时段

我先前写"关掉开关时促销班可排下午晚上三档，两个场景一致" —— **这是错的**。黄金时段只按时间定义，不代表可以忽略**当天开不开门**。两个过滤要**取交集**：

| 场景 · 星期 | 当天允许时段 | 减去黄金时段后，促销班可排 |
|---|---|---|
| 寒暑假 任意日 | 五档全开 | 14:00-16:00 · 16:20-18:20 · 18:30-20:30 |
| 春秋季 周六/周日 | 五档全开 | 14:00-16:00 · 16:20-18:20 · 18:30-20:30 |
| **春秋季 周五** | **只有 18:30-20:30** | **只有 18:30-20:30**（本来就不是黄金档，不受影响） |

### S6 是条件性硬限制，不是软罚分

用户原话是"不允许则**不排**黄金时间段"—— 这是**排除**，不是"扣分但可以被别的目标抵消"。

所以 S6 **不该待在软约束表里**：开关关闭时，直接把黄金时段从促销班的候选集里**删掉**，让求解器根本看不见这些格子；开关打开时，只是不做这道过滤 —— **不是**反过来奖励促销班去占黄金时段。

| 开关 | 行为 |
|---|---|
| `false` | 促销班的候选时段集合 = 当天允许时段 − 黄金时段（硬排除） |
| `true` | 不做过滤。促销班和常规班一视同仁，**没有优先权** |

### 验收用例

1. 春秋季周五 + 开关关闭 → 促销班仍可排 18:30-20:30（唯一一档，且不是黄金档）
2. 春秋季周六 + 开关关闭 → 促销班**不得**出现在 08:10-10:10 和 10:30-12:30，任何权重组合下都不行
3. 开关打开 → 促销班可以出现在黄金档，但不因为是促销班就被优先排进去

---

## 5. 目标函数

硬约束进模型当约束，软约束进目标函数加权求和，权重全部可配：

```
minimize  w1 × 彻底排不上的团队数            （压倒性权重）
        + w2 × 学生撮被拆到两个期位的撮数    （§4.5 —— 比排不上轻得多）
        + w3 × 期位与历史不符的团队数        （§4.5.3 延续性）
        + w4 × 连堂未达标的对数（按 §4 S1 三档分别计分）
        + w5 × 各期「年级 × 科目」未覆盖的组合数   （§4.5 S7）
        + w6 × 指导员当日转场次数
        + w7 × 转场里程分
        + w8 × 指导员课表空档段数
        + w9 × 工作量峰值
        + w11 × 同校区同程度产品并发的受影响学生数   （§4.6 S8 —— 原 H2 降级）
        + w12 × 期位 B（周日）上的团队数              （§4.6 S9 —— 周六优先）
        + w13 × 同校区同程度只有单边期位有班的组合数  （§4.6 S10）
        + w14 × 指导员在同一中心的额外使用教室数        （§4.6.1 S11，distinct−1 口径）
        + w10 × 与原排班不同的格子数         （仅 repair 模式）
```

权重必须满足 §4.5.5 的严格序：`w1 >> w2 > w3 > w4`。w2–w5 和 S7 是本次新增的，代码里还不存在（缺陷 R6）。

> **配置陷阱**【已验证 · 表述按 codex-002 更正】：`weights_from()` 的实现是 `dict(DEFAULT_WEIGHTS)` 再 `.update(cfg["权重"])` —— **配置里的同名键覆盖代码默认值，未提供的键仍保留默认**，不是丢掉整张默认表。
> 历史故障：连续三轮调 `跨中心一次`（20→150→800）完全不生效，因为 `rules.yaml` 里那个键还写着 20。发现方式是报告里 `跨中心 460 = 23 × 20`。
> **必须做**：加载后打印实际生效的权重表。

副产品：有了这个打分函数，就能**给现有手工排班打分**，量化"工具比手排好了多少"。

---

## 6. 输入 / 输出

### 6.1 输入

**① 待排团队清单** —— 要开哪些课
```
期位 | 中心 | 程度 | 产品 | 团队类型 | 平行班数量 | 是否促销 | 本团队服务次数
```

**①b 各科目组合的学生人数**（滚动估计值，见 §4.8）
```
校区 | 程度 | 团队类型 | 科目组合（如 数学+物理） | 人数
```
S8 的惩罚力度和 S1 的配对优先级靠它。**不追求精确** —— 缺了就退回默认顺序，有了也只作为权重的比例依据。

**② 指导员配置** 【用户定案：做成后台配置，支持批量改和单个改】
```
主指导员 | 可教产品 | 可去的中心（或区域） | 不可用的星期/时段 | 单日最多几节 | 是否不可调整
```
候选池口径三选一：`不限`（31 候选）/ `就近`（9）/ `历史`（3）。默认 `就近`。

**③ 场地**
```
中心 | 教室数 | 各教室座位数
```

**④ 规则参数**（yaml，改规则不动代码）
```
场景：寒暑假 | 春秋季
时段表、冲突口径分组字段、连堂规则与优先级、
地理判定方式（时间/距离）、单人单日上限、各项权重
```

**⑤ 学年日历** 【用户定案】
```
批次名（寒假/暑假/春季/秋季） | 起始日期 | 结束日期 | 期位A的日期集合 | 期位B的日期集合
```
批次就是寒假 / 暑假 / 春季 / 秋季。具体日期每年跟着学校放假时间走，**做成可配置**。
填了日历才能把排班展开成真实日期，查出跨批次的指导员撞车（R1）。

**⑥ 中心坐标** —— 自动反查 + 手动补录，见 §6.4

### 6.2 输出

1. **排班明细表** —— 列结构与现有导出一致，可直接回灌体检做 diff
2. **看板透视** —— 行 = 区域/校区，列 = 段次 × 时段，格子里 `程度/类型/产品简称/主指导员/人数`
3. **指导员课表 + 工作量统计**
4. **冲突 & 未排报告** —— 哪些团队没排上、为什么（缺老师？缺教室？时段冲突？），牺牲了哪些软约束

### 6.3 两种模式

- `rebuild` —— 从零重排
- `repair` —— 在现有排班上做最小改动（学期中途只能修不能推翻），额外惩罚"与原排班不同"

### 6.4 中心坐标的获取 【用户定案】

三层，缺一不可：

1. **自动反查** —— 由细到粗试四种问法（详细地址 → 城市+中心名 → 城市+区域+校区名 → 城市+校区名），命中就停。落在深圳范围外的丢弃，宁可留空也不写错坐标。
2. **手动补录入口** —— 反查不到的，页面上给一个入口：点开跳转到地图，用户在地图上找到位置、**复制经纬度粘回来**即可。不需要用户懂坐标系。
3. **顶部常驻告警** —— 只要还有中心没坐标，网页顶部挂一条固定的提示条（warning 样式），列出缺哪几个、点一下就去补。

> **初始化时就要引导用户做完这件事。** 缺坐标不会报错，只会让"赶不赶得及"的判定悄悄退化成三档粗判 —— 这种沉默的降级最危险，所以必须在界面上一直喊。

### 6.5 指导员配置 【用户定案】

后台配置页，**支持批量改和单个改**：

```
姓名 | 可教产品 | 可去的中心/区域 | 不可用的星期/时段 | 单日最多节数 | 单日最多中心数 | 是否不可调整
```

排班时按这些属性算可行性，不需要在别处再配一遍。缺配置的指导员同样要在顶部告警条里列出来。

---

## 7. 已知缺陷（待修，已与 Codex 交叉核对）

| # | 缺陷 | 影响 | 状态 |
|---|---|---|---|
| **R1** | `(段次, 周)` 被当成互不相干的子问题，但批次在日期上可能重叠且共用教师 | 原表有 **21 处估算重叠候选**未被检出。⚠️ 这是按 `最后服务日期 − (次数−1)` **推算**的日期区间得出的，**不是已验证的真实撞车** —— 学年区间有交集 ≠ 实际授课日重叠。所有 "H1 = 0" 的结论只在批次内成立 | 待修。用户确认没有原课表，这 21 处**需真实授课日期才能核查**；学年日历改为输入项 |
| **R2** | 连堂语义与 §4 S1 相反：代码把跨午休判为不连堂 | 与用户定案冲突，连堂达标率被低估 | 待改写为三档偏好 |
| **R3** | 连堂配对优先级（数学+语文 > 数学+英语 > 语文+英语）未实现 | 排出来的连堂组合可能不是业务想要的 | 待实现 |
| **R4** | 转场计数口径不统一：求解算 distinct−1，体检算实际事件 | 同一份排班两个数（19 vs 25），无法互相验证 | 待统一到"实际转场事件" |
| **R5** | 时段表写死在配置的单一 `时段` 列表里 | 无法表达"春秋季周五只开一档" | 待改为按场景 + 星期取时段 |
| **R12** | **`derive_rooms()` 的 `groupby(["中心","段次","周"])` 沿用 pandas `dropna=True`，`段次`/`周` 为空的行被静默丢弃** | 该中心若同时没有有效教室标签，就在 `rooms` 里**完全缺键**，`_room_capacity()` 的 `if limit is None: continue` 直接跳过 —— 容量约束整个消失。**与 R11 是同一种失效模式，换了个 pandas 默认值**。`load_schedule()` 只保证 `首次服务时间` 非空，不保证这两列非空，因此可达；而 `solve()` 按 `(段次, 周)` 分组时 NaN 会自成一组，团队照样被排上，只是那个中心没有教室上限 | 待修。**先前已存在，非 R10/R11 修复引入**，不阻塞该 PR。修法：`groupby(..., dropna=False)`，或在 `load_schedule()` 里把这两列的空值显式归一为哨兵值 |
| **R11** | **`derive_rooms()` 对 None/NaN 教室标签派生出 0 间，进而让 H3 对该中心完全失效** | 过滤器用 `astype(str)`（None 变成非空字符串，通过筛选），计数器用原列 `.nunique()`（None 被 dropna 掉，得 0）—— **同一列的两种视图不一致**。派生出 0 后 `rooms.setdefault()` 因键已存在而不补历史峰值，`_room_capacity()` 又 `if not limit: continue`，整个中心的容量约束消失。Codex 报告本批次 18 个中心有 15 个派生为 0（该统计我方无数据无法复核） | ✅ **已修复** —— Codex 在 `codex/fix-room-capacity`（`cbf2131`），我方独立审查通过，PR #1。我方三条修正已全部落到代码与测试，见 §7.2 |
| **R10** ✅ | **H3 按时段标签计数，不按真实时间重叠计数**（已修复，见 PR #1） | `_room_capacity()` 对每个时段标签独立求和，两个**标签不同但时间重叠**的班可以同时超额占用教室而不被发现。与当年 H2 那个 `p1 < p2` 单向判定是同一类 bug。Codex 已用合成用例证实（1 间教室 + 13:30-15:30 与 14:00-16:00 两个班 → 旧模型错误放行） | 待修。13:30 档虽已从默认配置移除，但时段表可配置，**重叠仍可被表达**，所以这是活的缺陷不是历史遗留 |
| **R7** | **H2 被建成硬约束** | 与业务不符（见 §3.1），必须改。⚠️ **但「H2 是不收敛的根因」这条推断已被诊断降级为「未证实」** —— H2 确实是可行域的真实限制，但没有任何一组在 30 秒内证明全局最优。不能把先前的 31 次转场、140 秒不收敛归因于它。见 §7.1 | 降级为软约束 S8，惩罚正比于受影响学生数 |
| **R6** | **期位与降级链完全缺失**：没有「期位」这个维度，没有「挪到另一个期位」这个动作，也没有「一撮学生」这个建模对象 | §4.5 的全部规则都落不了地。现有实现只有"排上/没排上"两态，把推迟当成失败会让求解器过度让步 | 待实现。工作量最大的一条 |

---

### 7.1 Q14 诊断的结论边界 【codex-003，我方确认】

Codex 用固定系数代理罚分（**不是** S8，也不是真实受影响学生数）在段次3 上做了单因素对照。**结论必须严格限定**：

| 能说的 | 不能说的 |
|---|---|
| H2 确实是可行域的一项真实限制 —— 软化后找到了旧 H2 会排除的候选 | ❌ H2 硬约束下找不到 20 次或更少的方案（硬组**未证明最优**） |
| H2 的定义会影响搜索结果 | ❌ 放软能更快收敛（**没有一组在 30 秒证明全局最优**；单 seed 不能推普遍性） |
| 20 次是本次设置下找到的候选 | ❌ 20 次是新最优；❌ 20 与旧固定时段实验的 21 可直接比较（不同实验设置） |
| 高系数组返回 UNKNOWN | ❌ UNKNOWN = INFEASIBLE。已知可行起点 38566 在该软目标下经独立验证可行，**限时搜索没找到 ≠ 不存在** |
| — | ❌ 先前的 31 次转场、140 秒不收敛可归因于 H2 |
| — | ❌ 新业务模型 S8 排得更好（缺报读组合、真实教室容量与可用性、日历，根本无法回答） |

> **我撤回 claude-008 里的一条推断。** 我当时写「H2 被建成硬约束**很可能是**求解器不收敛的根因之一」。诊断不支持这个说法 —— 它只支持「H2 是可行域的真实限制」，两件事差得远。R7 仍然要修（它与业务定案不符），但**不能拿「修了就能收敛」当理由**。
>
> 另外这批诊断是**在故意保留 R10 与 R11 两个缺陷的模型上跑的**（为了隔离 H2 这一个变量）。修完之后必须**重新建立基线**，本次的 20 次结果**不能当作交付排班**。

### 7.2 R11 的三条修正 【我方独立复核】

Codex 的最小复现我实跑确认（`derive_rooms` 全 None → `{'C': 0}`，历史并发峰值本应是 2）。但跑了六个变体之后，有三处它的描述需要修正 —— 这些直接影响修法：

| 输入 | 实际返回 | 说明 |
|---|---|---|
| 全 `None` | **0** ⚠️ | 缺陷成立 |
| 全 `NaN` | **0** ⚠️ | 缺陷成立 |
| 全空白字符串 `"  "` | 2 ✅ | **本来就是对的** |
| 全空串 `""` | 2 ✅ | **本来就是对的** |
| 混合 `[None, "02"]` | **1** ⚠️ | **比返回 0 更危险，见下** |
| 正常 `["01","02"]` | 2 ✅ | — |

**修正一：空白字符串不在缺陷范围内。** Codex 写的是"排除 None/NaN/空白标签"，但空白和空串已经被 `.str.strip() != ""` 正确过滤掉、正常回退到峰值。只有 `None`/`NaN` 有问题。修的时候不要动已经对的那部分。

**修正二：根因不是"None 变成文字后仍入选"，是过滤器和计数器看的是同一列的两种视图。**

```
过滤：df["指导室"].astype(str).str.strip() != ""   → None 变成非空字符串，通过
计数：sub["指导室"].nunique()                      → None 被 dropna 掉，得 0
```

所以修法的关键不是"多排除一种空值"，而是**过滤与计数必须落在同一个归一化后的视图上**。只在过滤器里补 `None` 判断，混合情况仍然会错（见修正三）。

**修正三：混合情况返回 1，这比返回 0 更难发现、也更危险。**

两个团队、一个有标签一个 `None` → 返回 1 间。这个值**不会**触发 `if not limit: continue`，所以 H3 照常生效，只是**上限被静默低估**。

返回 0 至少让约束整个消失、在体检里显眼；返回 1 是悄悄把容量算小，会造出**假性不可行** —— 正是 Codex 自己在 Q27 里警告过的那一类。而且修完 R10（收紧）之后，这个低估会被放大：一个本来可行的实例可能因为教室数被算少而判不可行，然后被误归因给 R10 的收紧。

**建议加一条验收**：混合标签（部分有、部分为 None）的中心，派生间数不得小于该中心的历史并发峰值。

---

## 8. 状态清单 【按 codex-001 Q26 重构】

我在 claude-010 里宣称"U1–U18 全部结案、没有待定项"。**这个说法不成立**，它把三件不同的事混为一谈：

- **定了怎么采集** ≠ **数据已经录进来了**（选定坐标补录方案，不等于坐标已填）
- **定了做成配置** ≠ **功能已经实现了**（说好指导员属性配置化，不等于 H6 已生效）
- 而且 **U7、U9 从头到尾就没有答案**，我却把它们一起算进"全部结案"

以下按四种状态分开记。**只有第一栏是真的可以不管了。**

### 8.1 业务规则已定 ✅

产品↔科目↔年级 · 程度与层级口径 · 中心vs校区 · 期位（两场景统一、有延续性、可换） · 两个场景与时段表 · 连堂三档偏好与"只看当天" · 转场口径 · 降级链 · H2 降级为 S8 · 周六优先 · 新生进班与中途换班 · 不预留容量 · 教室固定为软偏好 · 促销班黄金时段为条件性硬排除 · 黄金时段定义 · 13:30 档暂移除 · 校区单日节数为派生量 · 批次=寒暑假/春秋季

### 8.2 采集方案已定，数据待录入 ⬜

| # | 数据 | 方案已定 | 现状 |
|---|---|---|---|
| D1 | **中心经纬度** | 自动反查 + 手动补录 + 顶部告警（§6.4） | **Claude 工作区未持有**（只有 `centers.example.csv`）。Codex 报告已取得 **20/30 个中心的 OSM 地名参考点**，未经门店认证；原求解涉及的 25 次转场中 15 次两端都有参考点、10 次仍缺。真实坐标表按协议不入库 |
| D2 | **指导员可用性/资质/不可调整** | 后台配置，批量+单个改（§6.5） | 无数据。H6 **未生效** |
| D3 | **学年日历** | 可配置，跟学校放假走 | 无数据。R1 的跨批次撞车**仍查不出来** |
| D4 | **各班当前人数与额定容量** | §4.8.1 第①步要用 | 无数据 |
| D5 | **哪些安排已锁定** | §4.8.1 第③步的可改范围 | 无数据 |

> 这五项**不阻塞开工**（可以先做功能后填数据），但**阻塞验收**：数据没填之前，任何"转场时间合理""无跨批次冲突"的结论都不成立。

### 8.3 仍然未知，且影响历史数据的导入与验证 ⚠️

| # | 未知 | 影响 |
|---|---|---|
| **U7** | 看板"人数"的口径。【推断】反算 `购买服务时长 ÷ (团队分钟 × 服务次数)`，84% 能整除、均值 15.9，像是"在读人数"，但**未经用户确认** | 人数是 S1/S8 权重的分母。口径错了，所有按人数加权的惩罚都偏 |
| **U9** | 原表 `段次1–4` 与期位的对应关系。【推断】可能是"常规两期+促销两期"，**未验证** | 决定能不能把 661 行历史数据映射进新模型。映射不了，就没有基线可对比，"工具比手排好多少"无从谈起 |
| **U21** | 各报读组合的估计人数（§4.5.6） | S1 连堂优先级、S8 并发惩罚、拆期代价全靠它。缺失时只能用历史班量做弱代理，并在报告里标注 |

> **这三条不能靠"做成可配置"解决** —— 可配置提供的是信息入口，不是信息本身。

### 8.4 功能待实现 🔧

见 §7 的 R1–R7，以及 §4.6.1 提出的教室实体改造。**R7（H2 放软）应最先做**：它是唯一一条可能让先前所有实验结论作废的缺陷。

---

## 9. 数据安全约束

- 原始 xlsx 含 **106 位真实指导员姓名**和门店信息，**不得提交进仓库**
- `.gitignore` 覆盖：`*.xlsx`、`config/rules.yaml`、`config/centers.csv`、`config/travel.csv`、`secrets/`、`*.pem`、`*.lic`、`dist/`、`build/`
- 协作消息里只放**去标识**的汇总、文件哈希或可公开核查的来源，**不放真人姓名**
- 开发用脱敏样例数据，字段结构见 §1、§6
